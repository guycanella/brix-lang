// Test: Type inference for generic functions
function add<T>(a: T, b: T) -> T {
    return a + b
}

// Call without type arguments - should infer T = int
var x := add(10, 20)

// Call without type arguments - should infer T = float (promotion)
var y := add(1, 2.5)

println(x)
println(y)
