// Test: Generic function with multiple type parameters
function add<T>(a: T, b: T) -> T {
    return a + b
}

// Call multiple times with different types (tests cache)
var x := add<int>(10, 20)
var y := add<int>(5, 15)
var z := add<float>(3.14, 2.86)

println(x)
println(y)
println(z)
