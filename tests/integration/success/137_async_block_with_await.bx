// Test: async { } block that awaits an async fn internally
// Expected output:
// 84

async fn double(x: int) -> int {
    return x * 2
}

async fn main() {
    var future := async {
        var x := await double(42)
        return x
    }
    var result := await future
    println(result)
}
