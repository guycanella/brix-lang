struct Point { x: int, y: int }
fn (p: Point) scale(n: int) -> Point { return Point { x: p.x * n, y: p.y * n } }
fn (p: Point) x_val() -> int { return p.x }
println(Point{ x: 3, y: 4 }.scale(2).x_val())
