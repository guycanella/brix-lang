import math

// Solve A*x = b with A = diag(2,3), b = [4,9] (a 1xN Brix row vector) -> x = [2,3].
// x is returned in b's shape (1xN).
var A := zeros(2, 2)
A[0][0] := 2.0
A[1][1] := 3.0

var b := [4.0, 9.0]
var x := math.solve(A, b)

println(x[0])   // 2
println(x[1])   // 3
