// Queue<T> (v1.8 Grupo D) — FIFO basics: enqueue/dequeue/size/is_empty.
var q := Queue<int>()
q.enqueue(1)
q.enqueue(2)
q.enqueue(3)
println(q.size())
println(q.is_empty())
println(q.dequeue() ?: -1)
println(q.dequeue() ?: -1)
println(q.size())
