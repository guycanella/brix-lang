// Phase 2c: map on 2D Matrix without explicit return type annotation
var m := zeros(2, 3)
var result := m.map((x: float) -> { return x + 1.0 })
println(result.rows)
println(result.cols)
println(result[0])
