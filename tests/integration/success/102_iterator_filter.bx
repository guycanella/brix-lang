// Test: filter on IntMatrix
var nums := [1, 2, 3, 4, 5, 6]
var evens := nums.filter((x: int) -> bool { return x % 2 == 0 })
println(evens.cols)
println(evens[0])
println(evens[2])

// filter keeping all
var all_pos := nums.filter((x: int) -> bool { return x > 0 })
println(all_pos.cols)
